USE employeeTracker_db;

-- add
INSERT INTO employee (first_name, last_name, role_id, manager_id)
VALUES ("Jane", "Lane", "4", null);

-- view
SELECT * FROM employee;

-- update
SELECT * FROM employee;

-- delete
SELECT * FROM employee;

